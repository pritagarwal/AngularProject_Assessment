export class Faculty_Prit {
    id: number
    userId: number
    name: string

    constructor(id: number,
        userId: number, name:string)
    {
        this.id = id
        this.userId = userId
        this.name = name
    }
}